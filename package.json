{
  "name": "tasktracker",
  "version": "1.0.0",
  "description": "Full-stack task tracker application",
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  }
}
